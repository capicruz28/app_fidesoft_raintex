import 'package:flutter/material.dart';

// Módulo de avisos deshabilitado temporalmente.
// Este archivo se mantiene para evitar imports/rutas rotas en el proyecto,
// pero no se usa actualmente.
class AvisoPendienteScreen extends StatelessWidget {
  const AvisoPendienteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Módulo de avisos deshabilitado.'),
      ),
    );
  }
}

